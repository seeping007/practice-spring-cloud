spring:
  application:
    name: producer-service

server:
  port: 8001

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://admin:123456@127.0.0.1:8000/eureka/
    register-with-eureka: true
    fetch-registry: true